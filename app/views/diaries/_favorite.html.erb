      <% if current_user.favorited_by?(diary.id) %>
        <td><%= link_to 'いいねを外す', destroy_favorite_path(diary.id), method: :DELETE %></td>
      <% else %>
        <td><%= link_to 'いいね', create_favorite_path(diary.id), method: :POST %></td>
      <% end %>